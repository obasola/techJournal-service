/*
 * Created on 1 Jul 2016 ( Time 19:35:42 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.kumasi.techjournal.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.kumasi.techjournal.web.common.AbstractController;
import com.kumasi.techjournal.web.common.FormMode;
import com.kumasi.techjournal.web.common.Message;
import com.kumasi.techjournal.web.common.MessageType;

//--- Entities
import com.kumasi.techjournal.domain.SourceCode;
import com.kumasi.techjournal.domain.SourceType;
import com.kumasi.techjournal.domain.TechNote;

//--- Services 
import com.kumasi.techjournal.business.service.SourceCodeService;
import com.kumasi.techjournal.business.service.SourceTypeService;
import com.kumasi.techjournal.business.service.TechNoteService;

//--- List Items 
import com.kumasi.techjournal.web.listitem.SourceTypeListItem;
import com.kumasi.techjournal.web.listitem.TechNoteListItem;

/**
 * Spring MVC controller for 'SourceCode' management.
 */
@Controller
@RequestMapping("/sourceCode")
public class SourceCodeController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "sourceCode";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "sourceCode/form";
	private static final String JSP_LIST   = "sourceCode/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/sourceCode/create";
	private static final String SAVE_ACTION_UPDATE   = "/sourceCode/update";

	//--- Main entity service
	@Resource
    private SourceCodeService sourceCodeService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private SourceTypeService sourceTypeService; // Injected by Spring
	@Resource
    private TechNoteService techNoteService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public SourceCodeController() {
		super(SourceCodeController.class, MAIN_ENTITY_NAME );
		log("SourceCodeController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "SourceType"
	 * @param model
	 */
	private void populateListOfSourceTypeItems(Model model) {
		List<SourceType> list = sourceTypeService.findAll();
		List<SourceTypeListItem> items = new LinkedList<SourceTypeListItem>();
		for ( SourceType sourceType : list ) {
			items.add(new SourceTypeListItem( sourceType ) );
		}
		model.addAttribute("listOfSourceTypeItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "TechNote"
	 * @param model
	 */
	private void populateListOfTechNoteItems(Model model) {
		List<TechNote> list = techNoteService.findAll();
		List<TechNoteListItem> items = new LinkedList<TechNoteListItem>();
		for ( TechNote techNote : list ) {
			items.add(new TechNoteListItem( techNote ) );
		}
		model.addAttribute("listOfTechNoteItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param sourceCode
	 */
	private void populateModel(Model model, SourceCode sourceCode, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, sourceCode);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfSourceTypeItems(model);
			populateListOfTechNoteItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfSourceTypeItems(model);
			populateListOfTechNoteItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of SourceCode found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<SourceCode> list = sourceCodeService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new SourceCode
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		SourceCode sourceCode = new SourceCode();	
		populateModel( model, sourceCode, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing SourceCode
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		SourceCode sourceCode = sourceCodeService.findById(id);
		populateModel( model, sourceCode, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param sourceCode  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid SourceCode sourceCode, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				SourceCode sourceCodeCreated = sourceCodeService.create(sourceCode); 
				model.addAttribute(MAIN_ENTITY_NAME, sourceCodeCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, sourceCode.getId() );
			} else {
				populateModel( model, sourceCode, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "sourceCode.error.create", e);
			populateModel( model, sourceCode, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param sourceCode  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid SourceCode sourceCode, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				SourceCode sourceCodeSaved = sourceCodeService.update(sourceCode);
				model.addAttribute(MAIN_ENTITY_NAME, sourceCodeSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, sourceCode.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, sourceCode, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "sourceCode.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, sourceCode, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			sourceCodeService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "sourceCode.error.delete", e);
		}
		return redirectToList();
	}

}
