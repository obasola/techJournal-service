/*
 * Created on 4 Jul 2016 ( Time 10:15:05 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package com.kumasi.techjournal.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import com.kumasi.techjournal.web.common.AbstractController;
import com.kumasi.techjournal.web.common.FormMode;
import com.kumasi.techjournal.web.common.Message;
import com.kumasi.techjournal.web.common.MessageType;

//--- Entities
import com.kumasi.techjournal.domain.TechNote;
import com.kumasi.techjournal.domain.Sprint;
import com.kumasi.techjournal.domain.Application;

//--- Services 
import com.kumasi.techjournal.business.service.TechNoteService;
import com.kumasi.techjournal.business.service.SprintService;
import com.kumasi.techjournal.business.service.ApplicationService;

//--- List Items 
import com.kumasi.techjournal.web.listitem.SprintListItem;
import com.kumasi.techjournal.web.listitem.ApplicationListItem;

/**
 * Spring MVC controller for 'TechNote' management.
 */
@Controller
@RequestMapping("/techNote")
public class TechNoteController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "techNote";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "techNote/form";
	private static final String JSP_LIST   = "techNote/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/techNote/create";
	private static final String SAVE_ACTION_UPDATE   = "/techNote/update";

	//--- Main entity service
	@Resource
    private TechNoteService techNoteService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private SprintService sprintService; // Injected by Spring
	@Resource
    private ApplicationService applicationService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public TechNoteController() {
		super(TechNoteController.class, MAIN_ENTITY_NAME );
		log("TechNoteController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "Sprint"
	 * @param model
	 */
	private void populateListOfSprintItems(Model model) {
		List<Sprint> list = sprintService.findAll();
		List<SprintListItem> items = new LinkedList<SprintListItem>();
		for ( Sprint sprint : list ) {
			items.add(new SprintListItem( sprint ) );
		}
		model.addAttribute("listOfSprintItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Application"
	 * @param model
	 */
	private void populateListOfApplicationItems(Model model) {
		List<Application> list = applicationService.findAll();
		List<ApplicationListItem> items = new LinkedList<ApplicationListItem>();
		for ( Application application : list ) {
			items.add(new ApplicationListItem( application ) );
		}
		model.addAttribute("listOfApplicationItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param techNote
	 */
	private void populateModel(Model model, TechNote techNote, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, techNote);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfSprintItems(model);
			populateListOfApplicationItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfApplicationItems(model);
			populateListOfSprintItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of TechNote found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<TechNote> list = techNoteService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new TechNote
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		TechNote techNote = new TechNote();	
		populateModel( model, techNote, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing TechNote
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		TechNote techNote = techNoteService.findById(id);
		populateModel( model, techNote, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param techNote  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid TechNote techNote, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				TechNote techNoteCreated = techNoteService.create(techNote); 
				model.addAttribute(MAIN_ENTITY_NAME, techNoteCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, techNote.getId() );
			} else {
				populateModel( model, techNote, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "techNote.error.create", e);
			populateModel( model, techNote, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param techNote  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid TechNote techNote, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				TechNote techNoteSaved = techNoteService.update(techNote);
				model.addAttribute(MAIN_ENTITY_NAME, techNoteSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, techNote.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, techNote, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "techNote.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, techNote, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			techNoteService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "techNote.error.delete", e);
		}
		return redirectToList();
	}

}
